SECRET_KEY="yours-super-secret-key"
DB_ENGINE=django.db.backends.postgresql
DB_NAME=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DB_HOST=postgres
DB_PORT=5432
PROD_DB= необязательный параметр, если его не определять,
        то будет использоваться sqlite3 база,
        если выставить False - postgresql
DEBUG= необязательный параметр, по дефолту стоит True
        письма отправляются локально в файл,
        False - письма отправляются на реальную почту(в папку спам:)
